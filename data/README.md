# Data

This folder is for synthetic or local hospital quality datasets.

Generate the default synthetic dataset with:

```powershell
python -m hospital_quality_dashboard.data --output data/hospital_quality_synthetic.csv --rows 12000
```

Do not commit real patient data or protected health information.

